box: ewtroan-pendo/goae@0.0.10
build:
  steps:
      - setup-go-workspace
      - add-ssh-key:
          keyname: TIMEOFF_KEY
      - script:
          name: go get
          cwd: program/
          code: |
            cd $WERCKER_SOURCE_DIR
            go version
            go get ./...
  
      - script:
          name: Build
          code: |
              go build