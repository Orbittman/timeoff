box: wercker/golang
build:
  steps:
      - add-ssh-key:
          keyname: werckeraccess
      - setup-go-workspace
      - script:
          name: Get dependencies
          cwd: program/
          code: |-
              go get
  
      - script:
          name: Build
          code: |
              go build