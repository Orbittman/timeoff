box: michilu/goapp
build:
  steps:
      - setup-go-workspace
          source-dir: program/
      - script:
          name: go get
          code: |
            go get
  
      - script:
          name: Build
          code: |
              go build
deploy:
  steps:
    - setup-go-workspace
        source-dir: program/
    - pjvds/step-go-appengine-deploy:
        email: tim@trashcompactor.co.uk
        password: $APP_ENGINE_PASS