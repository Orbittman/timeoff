box: pjvds/golang
build:
  steps:
      - setup-go-workspace
  
      - script:
          name: Get dependencies
          cwd: program/
          code: |-
              go get
  
      - script:
          name: Build
          cwd: program/
          code: |
              go build
