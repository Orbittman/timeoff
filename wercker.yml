box: wercker/golang
build:
  steps:
      - setup-go-workspace
  
      - script:
          name: Get dependencies
          cwd: program/
          code: |-
              go get
  
      - script:
          name: Build
          code: |
              go build