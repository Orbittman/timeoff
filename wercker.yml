box: wercker/golang
build:
  steps:
      - setup-go-workspace
      - add-ssh-key:
	    keyname: WerckerAccess
      - script:
          name: Get dependencies
          cwd: program/
          code: |-
              go get
  
      - script:
          name: Build
          code: |
              go build